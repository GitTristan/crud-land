<h1>All Users</h1>

  <%= link_to "New User", new_user_path %><br />
    <br></br>
  <% @users.each do |u| %>
  <%= link_to a.email, user_path(u) %><br />
  <%= link_to "Delete", user_path(u), method: :delete, data: {confirm: "Are you sure?"} %>


  <br />
  <% end %>
